<nav class="navbar is-transparent is-fixed-top">
  <div class="container">
    <div class="navbar-brand">
      <a class="navbar-item" href="/home">
      <img src="/logo.png">
    </a>
    </div>

    <div class="navbar-start">
      <div class="navbar-item">
        <div class="field has-addon">
          <%= form_tag pages_path, :method => 'get' do %>
              <%= text_field_tag :search, params[:search], :class => "input", :style => "width: 250px", :placeholder => "Search companies, positions, locations" %>
              <%= submit_tag 'Search', :class => "button is-info" %>
          <% end %>
        </div>
      </div>
    </div>

    <div class="navbar-end">
      <a class="navbar-item" href="/home">Home</a></li>
      <a class="navbar-item" href="/explore">Explore</a></li>
      <a class="navbar-item" href="#">Notifications</a></li>
      <div class="navbar-item has-dropdown is-hoverable">
        <div class="navbar-link">
          <%= current_user.username %>
        </div>
        <div class="navbar-dropdown is-right">
          <div class="navbar-item">
            <a href="/user/<%= current_user.username %>">My Profile</a>
          </div>
          <div class="navbar-item">
            <%= link_to('Messages', conversations_path) %>
          </div>
          <div class="navbar-item">
            <%= link_to('My Rankings', jobs_path) %>
          </div>
          <div class="navbar-item">
            <%= link_to('Settings', edit_user_registration_path) %>
          </div>
          <div class="navbar-item">
            <a href="#">Help</a>
          </div>
          <hr class="navbar-divider">
          <div class="navbar-item">
            <%= link_to('Log Out', destroy_user_session_path, :method => :delete) %>
          </div>
        </div>
      </div>
    </div>

  </div>
</nav>
