<div class="container is-fluid margins">
  <p class="header">Search Results</p>
  <% if @jobs.any? %>
    <% @jobs.each do |job| %>
      <%= render partial: 'layouts/fetchOtherRankings', locals: { :job => job } %>
    <% end %>
    <br>

  <% else %>
    <p>Sorry, no results.</p>
  <% end %>
</div>
