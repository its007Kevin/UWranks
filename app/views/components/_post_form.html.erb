<%= form_for(@newPosts) do |f| %>
  <div class="field">
    <div class="control">
      <%= f.text_field :content, class: "input", :placeholder => "Ask a question about this job" %>
    </div>
		<%= f.hidden_field :job_id, :value => job_id %>
    <br>
    <div class="control">
      <%= f.submit "Add Post", class: "button is-link" %>
    </div>
  </div>
<% end %>
